// Code generated by goplus from project_test.gp. DO NOT EDIT.
//goplus:v v0.28.0

package cli

import (
	"bytes"
	"os"
	"path/filepath"
	"strings"
	"testing"

	"goforge.dev/refine/native"
	"goforge.dev/refine/release"
)

func nativeBundleFixture(t *testing.T, namespace string) string {
	t.Helper()
	schema := `{"$id":"https://example.test/thing.json","title":"Thing","type":"string"}`
	imported, err := native.IngestProject(native.JSONSchema, []byte(schema), native.ProjectOptions{ResourceID: "https://example.test/thing.json", Root: native.ResourceSelector{TypeName: "Thing"}, Metadata: native.WireMetadata{PublicationNamespace: namespace}})
	if err != nil {
		t.Fatal(err)
	}
	bundle, err := imported.Bundle()
	if err != nil {
		t.Fatal(err)
	}
	return string(bundle)
}

func projectFixture(t *testing.T) string {
	t.Helper()
	root := t.TempDir()
	for _, version := range []string{"v0.1.0", "SNAPSHOT"} {
		file := filepath.Join(root, "schemata", "foo", version+".refine")
		if err := os.MkdirAll(filepath.Dir(file), 0755); err != nil {
			t.Fatal(err)
		}
		if err := os.WriteFile(file, []byte("package com.example\ntype Thing = String where length it > 0\n"), 0600); err != nil {
			t.Fatal(err)
		}
	}
	if err := os.WriteFile(filepath.Join(root, "pom.xml"), []byte("<project/>"), 0600); err != nil {
		t.Fatal(err)
	}
	return root
}

func TestProjectCLIAllVersionsAndCheck(t *testing.T) {
	root := projectFixture(t)
	var out, stderr bytes.Buffer
	args := []string{"project", "generate", "--root", root, "--json"}
	if code := Run(args, nil, &out, &stderr); code != 0 {
		t.Fatalf("generation %d: %s %s", code, &out, &stderr)
	}
	for _, version := range []string{"v0_1_0", "snapshot"} {
		javaPath := filepath.Join(root, "target", "generated-sources", "refine", "com", "example", "foo", version, "Thing.java")
		if _, err := os.Stat(javaPath); err != nil {
			t.Fatal(err)
		}
		for _, resource := range []string{"contract.refine", "explanation.md", "ordinary-json-schema.json", "refined-avro.json", "ordinary-openapi.json"} {
			if _, err := os.Stat(filepath.Join(root, "target", "generated-resources", "refine", "refine", "foo", version, resource)); err != nil {
				t.Fatal(err)
			}
		}
	}
	manifest := filepath.Join(root, ".refine-generated.json")
	before, err := os.ReadFile(manifest)
	if err != nil {
		t.Fatal(err)
	}
	out.Reset()
	if code := Run(append(args, "--check"), nil, &out, &stderr); code != 0 {
		t.Fatal(code, out.String(), stderr.String())
	}
	after, err := os.ReadFile(manifest)
	if err != nil || !bytes.Equal(before, after) {
		t.Fatal("check changed manifest")
	}
	edited := filepath.Join(root, "target", "generated-sources", "refine", "com", "example", "foo", "snapshot", "Thing.java")
	if err := os.WriteFile(edited, []byte("user edit"), 0600); err != nil {
		t.Fatal(err)
	}
	out.Reset()
	if Run(args, nil, &out, &stderr) != 1 {
		t.Fatal("generation overwrote edited generated source")
	}
	content, err := os.ReadFile(edited)
	if err != nil || string(content) != "user edit" {
		t.Fatal("user edit lost")
	}
}

func TestProjectCLIConfigNoCodegenAndOverrides(t *testing.T) {
	root := projectFixture(t)
	config := `{"families":{"foo":{"noCodegen":["v0.1.0"]}}}`
	if err := os.WriteFile(filepath.Join(root, "refine.project.json"), []byte(config), 0600); err != nil {
		t.Fatal(err)
	}
	writePublicationRecord(t, root, "foo", "0.1.0", release.PublicationUnpublished, nil)
	var out, stderr bytes.Buffer
	if Run([]string{"project", "generate", "--root", root, "--package", "org.override", "--output", "generated", "--flat"}, nil, &out, &stderr) != 0 {
		t.Fatal(out.String(), stderr.String())
	}
	data, err := os.ReadFile(filepath.Join(root, "generated", "Thing.java"))
	if err != nil || !strings.Contains(string(data), "package org.override.foo.snapshot;") {
		t.Fatal("package/layout override ignored", err)
	}
	if _, err := os.Stat(filepath.Join(root, "target", "generated-resources", "refine", "refine", "foo", "v0_1_0", "contract.refine")); err != nil {
		t.Fatal("no-codegen incorrectly removed schema resources", err)
	}
}

func TestProjectCLIConfiguredWireMetadata(t *testing.T) {
	root := releaseFixture(t, map[string]string{"schemata/id/SNAPSHOT.refine": "type Identifier = Int\n"}, `{"families":{"id":{"formats":["json-schema"],"wire":{"Scalars":{"Identifier":{"Kind":"decimal-string"}}}}}}`)
	var output, errors bytes.Buffer
	if status := Run([]string{"project", "generate", "--root", root}, nil, &output, &errors); status != 0 {
		t.Fatalf("wire configuration failed %d: %s %s", status, output.String(), errors.String())
	}
	schema, err := os.ReadFile(filepath.Join(root, "target/generated-resources/refine/refine/id/snapshot/ordinary-json-schema.json"))
	if err != nil || !strings.Contains(string(schema), `"type": "string"`) {
		t.Fatalf("wire policy missing from native export: %s %v", schema, err)
	}
}

func TestProjectCLIInputAndConfigFailures(t *testing.T) {
	for _, config := range []string{`{"schemaDir":"../escape"}`, `{"families":{},"families":{}}`, `{"unsupported":true}`, `{"families":{"missing":{"root":"T"}}}`, `{"families":{"foo":{"noCodegen":["v99.0.0"]}}}`} {
		root := projectFixture(t)
		if err := os.WriteFile(filepath.Join(root, "refine.project.json"), []byte(config), 0600); err != nil {
			t.Fatal(err)
		}
		var out, stderr bytes.Buffer
		if Run([]string{"project", "generate", "--root", root}, nil, &out, &stderr) != 1 {
			t.Fatal("invalid config accepted", config)
		}
		if _, err := os.Stat(filepath.Join(root, ".refine-generated.json")); !os.IsNotExist(err) {
			t.Fatal("failed validation wrote outputs")
		}
	}
}

func TestProjectCLILoadsOfflineImports(t *testing.T) {
	root := t.TempDir()
	folder := filepath.Join(root, "schemata", "foo")
	if err := os.MkdirAll(folder, 0755); err != nil {
		t.Fatal(err)
	}
	files := map[string]string{"schemata/foo/SNAPSHOT.refine": "import \"../../common.refine\"\ntype Thing = ID", "common.refine": "type ID = String", "refine.project.json": `{"families":{"foo":{"root":"Thing"}}}`}
	for name, source := range files {
		if err := os.WriteFile(filepath.Join(root, filepath.FromSlash(name)), []byte(source), 0600); err != nil {
			t.Fatal(err)
		}
	}
	var out, stderr bytes.Buffer
	if Run([]string{"project", "generate", "--root", root}, nil, &out, &stderr) != 0 {
		t.Fatal(out.String(), stderr.String())
	}
	data, err := os.ReadFile(filepath.Join(root, "target", "generated-resources", "refine", "refine", "foo", "snapshot", "contract.refine"))
	if err != nil || !strings.Contains(string(data), "type ID") || strings.Contains(string(data), "import ") {
		t.Fatal("imports not bundled", err)
	}
}

func TestProjectLoaderIncludesNativeBundlesAndRejectsAmbiguousVersions(t *testing.T) {
	config := `{"package":"base.pkg","families":{"foo":{"formats":["json-schema"]}}}`
	root := releaseFixture(t, map[string]string{"schemata/foo/v1.0.0.refine": "type Thing = String\n", "schemata/foo/SNAPSHOT.refined.json": nativeBundleFixture(t, "native.published")}, config)
	input, err := loadProject(root, "refine.project.json", "override.pkg")
	if err != nil {
		t.Fatal(err)
	}
	if len(input.Contracts) != 2 {
		t.Fatalf("native bundle was silently omitted: %+v", input.Contracts)
	}
	var found bool
	for _, contract := range input.Contracts {
		if contract.Version == nil {
			found = true
			if contract.NativeProject == nil || contract.Program != nil || contract.RootType != "" {
				t.Fatalf("native contract was flattened or given an invented root: %+v", contract)
			}
			if contract.JavaPackage != "override.pkg" || contract.LogicalNamespace != "" {
				t.Fatalf("native package override priority changed: %+v", contract)
			}
			if len(contract.Formats) != 1 || contract.Formats[0] != native.JSONSchema {
				t.Fatalf("native origin format lost: %+v", contract.Formats)
			}
		}
	}
	if !found {
		t.Fatal("native snapshot missing")
	}
	if err = os.WriteFile(filepath.Join(root, "schemata/foo/SNAPSHOT.refine"), []byte("type Thing = String\n"), 0600); err != nil {
		t.Fatal(err)
	}
	if _, err = loadProject(root, "refine.project.json", ""); err == nil || !strings.Contains(err.Error(), "defined by both") {
		t.Fatalf("multi-extension version collision accepted: %v", err)
	}
}

func TestProjectLoaderNativeBundleOwnsRootAndWireMetadata(t *testing.T) {
	files := map[string]string{"schemata/foo/SNAPSHOT.refined.json": nativeBundleFixture(t, "native.published")}
	configs := []string{`{"families":{"foo":{"root":"Other","formats":["json-schema"]}}}`, `{"families":{"foo":{"formats":["json-schema"],"wire":{"PublicationNamespace":"override"}}}}`}
	for _, config := range configs {
		root := releaseFixture(t, files, config)
		if _, err := loadProject(root, "refine.project.json", ""); err == nil {
			t.Fatalf("native bundle authority override accepted: %s", config)
		}
	}
}

func TestProjectLoaderAllowsProspectiveNativeNoCodegenVersion(t *testing.T) {
	config := `{"families":{"foo":{"formats":["json-schema"],"noCodegen":["v0.1.0"],"release":{"change":"feature","intended":"0.1.0"}}}}`
	root := releaseFixture(t, map[string]string{"schemata/foo/SNAPSHOT.refined.json": nativeBundleFixture(t, "native.published")}, config)
	input, err := loadProject(root, "refine.project.json", "")
	if err != nil {
		t.Fatal(err)
	}
	if len(input.Contracts) != 1 || input.Contracts[0].NoCodegen {
		t.Fatalf("prospective exclusion incorrectly suppressed the snapshot: %+v", input.Contracts)
	}
}

func TestProjectCLIGeneratesNativeBundleWithoutDiscardingIt(t *testing.T) {
	original := nativeBundleFixture(t, "published.contracts")
	config := `{"families":{"foo":{"formats":["json-schema"]}}}`
	root := releaseFixture(t, map[string]string{"schemata/foo/SNAPSHOT.refined.json": original}, config)
	var out, stderr bytes.Buffer
	if code := Run([]string{"project", "generate", "--root", root}, nil, &out, &stderr); code != 0 {
		t.Fatalf("native project generation %d: %s / %s", code, &out, &stderr)
	}
	copied, err := os.ReadFile(filepath.Join(root, "target/generated-resources/refine/refine/foo/snapshot/contract.refined.json"))
	if err != nil || string(copied) != original {
		t.Fatalf("native bundle resource changed: %v", err)
	}
	if _, err = os.Stat(filepath.Join(root, "target/generated-sources/refine/published/contracts/foo/snapshot/Thing.java")); err != nil {
		t.Fatal("native publication namespace was not used", err)
	}
}

func TestMavenCLIIsOptInOutputOnly(t *testing.T) {
	var out, stderr bytes.Buffer
	if Run([]string{"project", "maven", "--executable", "/path/with spaces/refine"}, nil, &out, &stderr) != 0 {
		t.Fatal(stderr.String())
	}
	for _, want := range []string{"<maven.compiler.release>25", "<artifactId>maven-compiler-plugin", "<argument>project</argument>", "<argument>generate</argument>", "/path/with spaces/refine"} {
		if !strings.Contains(out.String(), want) {
			t.Error("missing", want)
		}
	}
	if Run([]string{"project", "maven"}, nil, brokenWriter{}, &stderr) != 2 {
		t.Fatal("ignored writer failure")
	}
}
