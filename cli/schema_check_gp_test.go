// Code generated by goplus from schema_check_test.gp. DO NOT EDIT.
//goplus:v v0.28.0

package cli

import (
	"bytes"
	"encoding/json"
	"strings"
	"testing"
)

func TestSchemaCheckCLIRejectsOnlyProvenEmptyAndReportsUnknown(t *testing.T) {
	for _, tc := range []struct {
		source      string
		status      int
		state, code string
	}{
		{"type T = Int where it >= 0", 0, "valid", ""},
		{"type T = Int where it > 0 where it < 1", 1, "invalid", "schema.unsatisfiable"},
		{"type T = [Int]", 0, "unknown", "analysis.unknown"},
		{"loop :: Int -> Bool\nloop n = loop n\ntype T = Int where loop it", 0, "unknown", "analysis.unknown"},
	} {
		var out, stderr bytes.Buffer
		status := Run([]string{"check-schema", "--json", "--total-steps", "20", "-"}, strings.NewReader(tc.source), &out, &stderr)
		if status != tc.status || stderr.Len() != 0 {
			t.Fatal(status, out.String(), stderr.String())
		}
		var got report
		if err := json.Unmarshal(out.Bytes(), &got); err != nil {
			t.Fatal(err)
		}
		if got.State != tc.state || got.Phase != "check-schema" || got.Result == nil {
			t.Fatal(got)
		}
		if tc.code != "" {
			found := false
			for _, detail := range got.Diagnostics {
				if detail.Code == tc.code {
					found = true
				}
			}
			if !found {
				t.Fatal("missing explicit proof diagnostic", got)
			}
		}
	}
	var out, stderr bytes.Buffer
	if Run([]string{"check-schema", "-"}, strings.NewReader("type T = [Int]"), &out, &stderr) != 0 || !strings.Contains(out.String(), "analysis.unknown") {
		t.Fatal("human output hid unknown", out.String(), stderr.String())
	}
}
