// Code generated by goplus from schema_limits_test.gp. DO NOT EDIT.
//goplus:v v0.28.0

package analysis

import (
	"testing"

	"goforge.dev/refine/language"
	"goforge.dev/refine/validation"
)

func TestAnalysisAndSyntaxEvidenceRetainSchemaLimits(t *testing.T) {
	base, err := language.Compile("type T = Int where it > 0\n")
	if err != nil {
		t.Fatal(err)
	}
	limited, err := language.Compile("@limits total 80 clause 20\ntype T = Int where it > 0\n")
	if err != nil {
		t.Fatal(err)
	}
	if got := analysisExpansionLimit(limited, validation.Limits{}); got != 20 {
		t.Fatalf("analysis ignored schema clause limit: %d", got)
	}
	if got := analysisExpansionLimit(limited, validation.Limits{Clause: 7}); got != 7 {
		t.Fatalf("analysis ignored tighter caller limit: %d", got)
	}
	evidence, err := CompareContractSyntax(base, "T", limited, "T")
	if err != nil || evidence.Equal || len(evidence.Differences) != 1 || evidence.Differences[0].Kind != "limits" || evidence.BaselineFingerprint == evidence.CandidateFingerprint {
		t.Fatalf("limit-only change escaped syntax identity: %+v %v", evidence, err)
	}
}
