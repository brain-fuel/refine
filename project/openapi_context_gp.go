// Code generated by goplus from openapi_context.gp. DO NOT EDIT.
//goplus:v v0.28.0

package project

import (
	"fmt"

	"goforge.dev/refine/java"
	"goforge.dev/refine/native"
)

const openAPIOperationsClass = "RefineOpenAPIOperations"

func projectWireConfigured(metadata native.WireMetadata) bool {
	return metadata.PublicationNamespace != "" || metadata.NumericExpansion != 0 || metadata.OpenAPI != nil || len(metadata.Scalars) > 0 || len(metadata.ExtraFields) > 0 || len(metadata.Discriminators) > 0
}

func contractJavaSources(contract Contract, namespace, class string, formats []native.Format) ([]java.File, error) {
	files, err := contractSerde(contract, namespace, class, formats)
	if err != nil {
		return nil, err
	}
	return mergeOpenAPIContext(contract, namespace, class, files)
}

// mergeOpenAPIContext deliberately regenerates the shared checked validator
// and requires byte-identical sources. A facade can never replace a model or
// serde runtime with a differently configured contract class.
func mergeOpenAPIContext(contract Contract, namespace, class string, files []java.File) ([]java.File, error) {
	if contract.Wire.OpenAPI == nil {
		return files, nil
	}
	context, err := java.GenerateOpenAPIContext(contract.Program, namespace, class, openAPIOperationsClass, *contract.Wire.OpenAPI)
	if err != nil {
		return nil, err
	}
	out := append([]java.File(nil), files...)
	seen := map[string]string{}
	for _, file := range out {
		seen[file.Path] = file.Source
	}
	for _, file := range context {
		if source, ok := seen[file.Path]; ok {
			if source != file.Source {
				return nil, fmt.Errorf("project.openapi: operation facade disagrees on shared Java file %s", file.Path)
			}
			continue
		}
		seen[file.Path] = file.Source
		out = append(out, file)
	}
	return out, nil
}
