// Code generated by goplus from write.gp. DO NOT EDIT.
//goplus:v v0.28.0

package project

import (
	"encoding/json"
	"errors"
	"fmt"
	"os"
	"path/filepath"
	"sort"
	"strings"

	"goforge.dev/refine/release"
	"goforge.dev/refine/schemajson"
)

type ownedManifest struct {
	Version int                          `json:"version"`
	Files   map[string]release.ContentID `json:"files"`
}
type OwnedAddition struct {
	Artifacts           []release.Artifact
	Replacements        []release.ReplacementArtifact
	Deletions           []release.DeletionArtifact
	ManifestCreate      *release.Artifact
	ManifestReplacement *release.ReplacementArtifact
}

var linkOwned = os.Link

func safeRelative(name string) (string, error) {
	if name == "" || filepath.IsAbs(name) {
		return "", fmt.Errorf("project.path: paths must be relative")
	}
	clean := filepath.Clean(name)
	if clean == "." || clean == ".." || strings.HasPrefix(clean, ".."+string(filepath.Separator)) {
		return "", fmt.Errorf("project.path: path escapes root")
	}
	if release.ReservedTransactionPath(clean) {
		return "", fmt.Errorf("project.path: reserved transaction path")
	}
	return clean, nil
}
func noSymlink(root, relative string, leaf bool) error {
	current := root
	parts := strings.Split(relative, string(filepath.Separator))
	limit := len(parts)
	if !leaf {
		limit--
	}
	for i := 0; i < limit; i++ {
		current = filepath.Join(current, parts[i])
		info, err := os.Lstat(current)
		if errors.Is(err, os.ErrNotExist) {
			continue
		}
		if err != nil {
			return err
		}
		if info.Mode()&os.ModeSymlink != 0 {
			return fmt.Errorf("project.symlink: %s", relative)
		}
		if i < limit-1 && !info.IsDir() {
			return fmt.Errorf("project.path: non-directory parent")
		}
	}
	return nil
}
func readManifest(root, name string) (ownedManifest, error) {
	manifest := ownedManifest{Version: 1, Files: map[string]release.ContentID{}}
	data, err := os.ReadFile(filepath.Join(root, name))
	if errors.Is(err, os.ErrNotExist) {
		return manifest, nil
	}
	if err != nil {
		return manifest, err
	}
	if _, err = schemajson.Parse(data, schemajson.Limits{}); err != nil {
		return manifest, fmt.Errorf("project.manifest: invalid or duplicate-key JSON: %w", err)
	}
	if err = json.Unmarshal(data, &manifest); err != nil || manifest.Version != 1 || manifest.Files == nil {
		return manifest, fmt.Errorf("project.manifest: invalid owned-output manifest")
	}
	for path, digest := range manifest.Files {
		clean, e := safeRelative(path)
		if e != nil || clean != path || !digest.Valid() {
			return manifest, fmt.Errorf("project.manifest: unsafe entry")
		}
	}
	return manifest, nil
}

// PlanOwnedAddition validates a complete generated bundle and prepares its
// immutable additions, content-bound replacements/deletions, and exact
// ownership-manifest transition for release.Promote. It does not write.
func PlanOwnedAddition(rootPath string, bundle Bundle, manifestPath string) (OwnedAddition, error) {
	root, err := filepath.Abs(rootPath)
	if err != nil {
		return OwnedAddition{}, err
	}
	rootInfo, err := os.Lstat(root)
	if err != nil || !rootInfo.IsDir() || rootInfo.Mode()&os.ModeSymlink != 0 {
		return OwnedAddition{}, fmt.Errorf("project.root: existing non-symlink directory required")
	}
	if manifestPath == "" {
		manifestPath = ".refine-generated.json"
	}
	manifestPath, err = safeRelative(manifestPath)
	if err != nil {
		return OwnedAddition{}, err
	}
	if err = noSymlink(root, manifestPath, true); err != nil {
		return OwnedAddition{}, err
	}
	old, err := readManifest(root, manifestPath)
	if err != nil {
		return OwnedAddition{}, err
	}
	next := map[string]release.ContentID{}
	folded := map[string]string{strings.ToLower(filepath.ToSlash(manifestPath)): manifestPath}
	for name, digest := range old.Files {
		portable := strings.ToLower(filepath.ToSlash(name))
		if prior, ok := folded[portable]; ok {
			return OwnedAddition{}, fmt.Errorf("project.collision: portable path %s collides with %s", name, prior)
		}
		folded[portable] = name
		if err = noSymlink(root, name, true); err != nil {
			return OwnedAddition{}, err
		}
		info, e := os.Lstat(filepath.Join(root, name))
		if e != nil || !info.Mode().IsRegular() {
			return OwnedAddition{}, fmt.Errorf("project.owned_missing: generated file %s disappeared", name)
		}
		data, e := os.ReadFile(filepath.Join(root, name))
		if e != nil {
			return OwnedAddition{}, e
		}
		if release.Digest(data) != digest {
			return OwnedAddition{}, fmt.Errorf("project.modified: generated file %s was edited", name)
		}
	}
	result := OwnedAddition{}
	desired := map[string]bool{}
	for _, file := range bundle.Files {
		relative, e := safeRelative(file.Path)
		if e != nil {
			return OwnedAddition{}, e
		}
		if desired[relative] {
			return OwnedAddition{}, fmt.Errorf("project.collision: duplicate output %s", relative)
		}
		desired[relative] = true
		if relative == manifestPath {
			return OwnedAddition{}, fmt.Errorf("project.collision: output cannot replace its ownership manifest")
		}
		portable := strings.ToLower(filepath.ToSlash(relative))
		if prior, ok := folded[portable]; ok && prior != relative {
			return OwnedAddition{}, fmt.Errorf("project.collision: portable path %s collides with %s", relative, prior)
		}
		folded[portable] = relative
		if e = noSymlink(root, relative, false); e != nil {
			return OwnedAddition{}, e
		}
		content := append([]byte(nil), file.Content...)
		digest := release.Digest(content)
		if oldDigest, owned := old.Files[relative]; owned {
			if oldDigest != digest {
				result.Replacements = append(result.Replacements, release.ReplacementArtifact{Path: relative, ExpectedContent: oldDigest, Content: content})
			}
			next[relative] = digest
			continue
		}
		if _, e = os.Lstat(filepath.Join(root, relative)); e == nil {
			return OwnedAddition{}, fmt.Errorf("project.collision: refusing existing output %s", relative)
		} else if !errors.Is(e, os.ErrNotExist) {
			return OwnedAddition{}, e
		}
		next[relative] = digest
		result.Artifacts = append(result.Artifacts, release.Artifact{Path: relative, Content: content})
	}
	for name, digest := range old.Files {
		if !desired[name] {
			result.Deletions = append(result.Deletions, release.DeletionArtifact{Path: name, ExpectedContent: digest})
		}
	}
	sort.Slice(result.Deletions, func(i, j int) bool { return result.Deletions[i].Path < result.Deletions[j].Path })
	manifestData, err := json.MarshalIndent(ownedManifest{Version: 1, Files: next}, "", "  ")
	if err != nil {
		return OwnedAddition{}, err
	}
	manifestData = append(manifestData, '\n')
	manifestFull := filepath.Join(root, manifestPath)
	current, err := os.ReadFile(manifestFull)
	if errors.Is(err, os.ErrNotExist) {
		created := release.Artifact{Path: manifestPath, Content: manifestData}
		result.ManifestCreate = &created
	} else if err != nil {
		return OwnedAddition{}, err
	} else {
		replacement := release.ReplacementArtifact{Path: manifestPath, ExpectedContent: release.Digest(current), Content: manifestData}
		result.ManifestReplacement = &replacement
	}
	return result, nil
}

// WriteOwned replaces only files proven unchanged since the preceding generated
// manifest. All new bytes are staged before any output changes. Returned install
// failures restore backups; process-crash atomicity is not promised.
func WriteOwned(rootPath string, bundle Bundle, manifestPath string) error {
	return WriteOwnedChecked(rootPath, bundle, manifestPath, nil)
}

// WriteOwnedChecked rechecks content-bound planning inputs after acquiring the
// shared project/release lock and before staging or deleting owned outputs.
func WriteOwnedChecked(rootPath string, bundle Bundle, manifestPath string, preconditions []release.FilePrecondition) error {
	root, err := filepath.Abs(rootPath)
	if err != nil {
		return err
	}
	rootInfo, err := os.Lstat(root)
	if err != nil || !rootInfo.IsDir() || rootInfo.Mode()&os.ModeSymlink != 0 {
		return fmt.Errorf("project.root: existing non-symlink directory required")
	}
	lock := filepath.Join(root, release.TransactionLockName)
	lockFile, err := os.OpenFile(lock, os.O_CREATE|os.O_EXCL|os.O_WRONLY, 0600)
	if err != nil {
		return fmt.Errorf("project.lock: %w", err)
	}
	_ = lockFile.Close()
	defer os.Remove(lock)
	if manifestPath == "" {
		manifestPath = ".refine-generated.json"
	}
	manifestPath, err = safeRelative(manifestPath)
	if err != nil {
		return err
	}
	if err = noSymlink(root, manifestPath, true); err != nil {
		return err
	}
	for _, condition := range preconditions {
		relative, e := safeRelative(filepath.FromSlash(condition.Path))
		if e != nil || !condition.Content.Valid() {
			return fmt.Errorf("project.precondition: invalid content-bound input %s", condition.Path)
		}
		if e = noSymlink(root, relative, true); e != nil {
			return e
		}
		info, e := os.Lstat(filepath.Join(root, relative))
		if e != nil || !info.Mode().IsRegular() {
			return fmt.Errorf("project.precondition: %s changed after planning", condition.Path)
		}
		data, e := os.ReadFile(filepath.Join(root, relative))
		if e != nil || release.Digest(data) != condition.Content {
			return fmt.Errorf("project.precondition: %s changed after planning", condition.Path)
		}
	}
	old, err := readManifest(root, manifestPath)
	if err != nil {
		return err
	}
	desired := map[string][]byte{}
	digests := map[string]release.ContentID{}
	folded := map[string]string{}
	for _, file := range bundle.Files {
		relative, e := safeRelative(file.Path)
		if e != nil {
			return e
		}
		if relative == manifestPath {
			return fmt.Errorf("project.collision: output cannot replace its ownership manifest")
		}
		portable := strings.ToLower(filepath.ToSlash(relative))
		if prior, ok := folded[portable]; ok {
			return fmt.Errorf("project.collision: portable paths %s and %s collide", prior, relative)
		}
		folded[portable] = relative
		if _, ok := desired[relative]; ok {
			return fmt.Errorf("project.collision: duplicate %s", relative)
		}
		if e = noSymlink(root, relative, false); e != nil {
			return e
		}
		desired[relative] = append([]byte(nil), file.Content...)
		digests[relative] = release.Digest(file.Content)
	}
	// Every existing destination and stale owned output is checked before stage.
	affected := map[string]bool{}
	for name := range desired {
		affected[name] = true
	}
	for name := range old.Files {
		affected[name] = true
	}
	for name := range affected {
		if e := noSymlink(root, name, true); e != nil {
			return e
		}
		full := filepath.Join(root, name)
		info, e := os.Lstat(full)
		if errors.Is(e, os.ErrNotExist) {
			if _, wasOwned := old.Files[name]; wasOwned {
				return fmt.Errorf("project.owned_missing: generated file %s disappeared", name)
			}
			continue
		}
		if e != nil {
			return e
		}
		if info.Mode()&os.ModeSymlink != 0 || !info.Mode().IsRegular() {
			return fmt.Errorf("project.collision: unsafe existing output %s", name)
		}
		data, e := os.ReadFile(full)
		if e != nil {
			return e
		}
		prior, wasOwned := old.Files[name]
		if !wasOwned {
			return fmt.Errorf("project.collision: refusing unowned output %s", name)
		}
		if release.Digest(data) != prior {
			return fmt.Errorf("project.modified: generated file %s was edited", name)
		}
	}
	stage, err := os.MkdirTemp(root, ".refine-project-stage-")
	if err != nil {
		return err
	}
	preserveStage := false
	defer func() {
		if !preserveStage {
			_ = os.RemoveAll(stage)
		}
	}()
	names := make([]string, 0, len(desired))
	for name := range desired {
		names = append(names, name)
	}
	sort.Strings(names)
	for i, name := range names {
		if err = os.WriteFile(filepath.Join(stage, fmt.Sprintf("new-%06d", i)), desired[name], 0644); err != nil {
			return err
		}
	}
	manifestData, err := json.MarshalIndent(ownedManifest{Version: 1, Files: digests}, "", "  ")
	if err != nil {
		return err
	}
	manifestData = append(manifestData, '\n')
	if err = os.WriteFile(filepath.Join(stage, "manifest"), manifestData, 0644); err != nil {
		return err
	}
	// Back up every owned file as a same-inode hard link before installation.
	affectedNames := make([]string, 0, len(affected))
	for name := range affected {
		affectedNames = append(affectedNames, name)
	}
	sort.Strings(affectedNames)
	backups := map[string]string{}
	for i, name := range affectedNames {
		full := filepath.Join(root, name)
		if _, e := os.Lstat(full); e == nil {
			backup := filepath.Join(stage, fmt.Sprintf("old-%06d", i))
			if e = linkOwned(full, backup); e != nil {
				return e
			}
			backups[name] = backup
		}
	}
	installed := []string{}
	rollback := func() error {
		for i := len(installed) - 1; i >= 0; i-- {
			name := installed[i]
			full := filepath.Join(root, name)
			if data, e := os.ReadFile(full); e == nil {
				wanted, keep := digests[name]
				if !keep || release.Digest(data) != wanted {
					return fmt.Errorf("project.rollback: concurrent replacement at %s", name)
				}
			}
			if e := os.Remove(full); e != nil && !errors.Is(e, os.ErrNotExist) {
				return e
			}
			if backup := backups[name]; backup != "" {
				if e := os.Rename(backup, full); e != nil {
					return e
				}
			}
		}
		return nil
	}
	fail := func(cause error) error {
		if rb := rollback(); rb != nil {
			preserveStage = true
			return errors.Join(cause, rb)
		}
		return cause
	}
	ensure := func(name string) error {
		parent := filepath.Dir(filepath.Join(root, name))
		return os.MkdirAll(parent, 0755)
	}
	for i, name := range names {
		if err = ensure(name); err != nil {
			return fail(err)
		}
		source := filepath.Join(stage, fmt.Sprintf("new-%06d", i))
		destination := filepath.Join(root, name)
		if _, owned := backups[name]; owned {
			err = os.Rename(source, destination)
		} else {
			err = linkOwned(source, destination)
		}
		if err != nil {
			return fail(err)
		}
		installed = append(installed, name)
	}
	// Remove stale owned paths only after every desired output is installed.
	for _, name := range affectedNames {
		if _, keep := desired[name]; keep {
			continue
		}
		if err = os.Remove(filepath.Join(root, name)); err != nil {
			return fail(err)
		}
		installed = append(installed, name)
	}
	if err = ensure(manifestPath); err != nil {
		return fail(err)
	}
	manifestFull := filepath.Join(root, manifestPath)
	manifestBackup := ""
	if _, e := os.Lstat(manifestFull); e == nil {
		manifestBackup = filepath.Join(stage, "old-manifest")
		if e = linkOwned(manifestFull, manifestBackup); e != nil {
			return fail(e)
		}
	}
	if err = os.Rename(filepath.Join(stage, "manifest"), manifestFull); err != nil {
		return fail(err)
	}
	_ = manifestBackup
	return nil
}
