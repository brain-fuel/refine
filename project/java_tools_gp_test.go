// Code generated by goplus from java_tools_test.gp. DO NOT EDIT.
//goplus:v v0.28.0

package project

import (
	"os"
	"path/filepath"
	"testing"
)

func writeProjectJavaTool(t *testing.T, root, name, version string) {
	t.Helper()
	path := filepath.Join(root, "bin", name)
	if err := os.MkdirAll(filepath.Dir(path), 0755); err != nil {
		t.Fatal(err)
	}
	source := "#!/bin/sh\necho '" + name + " " + version + "' >&2\n"
	if err := os.WriteFile(path, []byte(source), 0755); err != nil {
		t.Fatal(err)
	}
}

func TestProjectJavaToolsUseConfiguredHomesAndRequireJava25(t *testing.T) {
	refineHome := t.TempDir()
	javaHome := t.TempDir()
	writeProjectJavaTool(t, refineHome, "javac", "25.0.1")
	writeProjectJavaTool(t, refineHome, "java", "25.0.1")
	writeProjectJavaTool(t, javaHome, "javac", "24.0.2")
	writeProjectJavaTool(t, javaHome, "java", "24.0.2")
	t.Setenv("REFINE_JAVA_HOME", refineHome)
	t.Setenv("JAVA_HOME", javaHome)
	compiler, vm, err := projectJavaTools()
	if err != nil {
		t.Fatal(err)
	}
	if compiler != filepath.Join(refineHome, "bin", "javac") || vm != filepath.Join(refineHome, "bin", "java") {
		t.Fatalf("REFINE_JAVA_HOME did not take precedence: %s %s", compiler, vm)
	}
	t.Setenv("REFINE_JAVA_HOME", "")
	if _, _, err := projectJavaTools(); err == nil {
		t.Fatal("Java 24 was accepted")
	}
}
