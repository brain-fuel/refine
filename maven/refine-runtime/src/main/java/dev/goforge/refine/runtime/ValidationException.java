// Generated by Refine: Java 25 runtime. MIT licensed.
package dev.goforge.refine.runtime;

/** Thrown for both invalid and indeterminate normal validation boundaries. */
public final class ValidationException extends RuntimeException {
    private static final long serialVersionUID = 1L;
    private final Validation.Outcome outcome;
    public ValidationException(Validation.Outcome outcome) {
        super("Refinement validation failed: " + outcome.state().name().toLowerCase(java.util.Locale.ROOT));
        if (outcome.state() == Validation.State.VALID) throw new IllegalArgumentException("valid outcome cannot fail validation");
        this.outcome = outcome;
    }
    public Validation.Outcome outcome() { return outcome; }
    public java.util.List<Validation.Diagnostic> diagnostics() { return outcome.diagnostics(); }
}
