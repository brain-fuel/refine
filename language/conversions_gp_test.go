// Code generated by goplus from conversions_test.gp. DO NOT EDIT.
//goplus:v v0.28.0

package language

import (
	"testing"

	"goforge.dev/refine/validation"
)

func TestExplicitConversionsAndDurations(t *testing.T) {
	for _, test := range []struct {
		typ        string
		expression string
		want       string
	}{
		{"Real", "toReal 9007199254740993", "9007199254740993"},
		{"Result String Int", "toInteger 2.0", "(Ok 2)"},
		{"Result String Int", "toInteger 0.5", `(Err "conversion.fractional: exact integer conversion requires denominator one")`},
		{"Int", "floor (-1 / 3)", "-1"}, {"Int", "ceiling (-1 / 3)", "0"}, {"Int", "truncate (-1 / 3)", "0"},
		{"Int", "roundHalfEven (5 / 2)", "2"}, {"Int", "roundHalfEven (-7 / 2)", "-4"},
		{"[Int]", "map roundHalfEven [1.5, 2.5, -1.5, -2.5]", "[2, 2, -2, -2]"},
	} {
		e, result, failure := execution(t, "entry :: "+test.typ+"\nentry = "+test.expression, validation.Limits{})
		if failure != nil {
			t.Fatal(failure)
		}
		shown := e.show(result, Span{})
		if shown != test.want {
			t.Fatalf("%s = %s, want %s", test.expression, shown, test.want)
		}
	}
	for _, source := range []string{"entry :: Int\nentry = toInteger 1.0", "entry :: Real\nentry = toReal 1.0", "entry :: Bool\nentry = civilSecondsUntil \"a\" \"b\""} {
		if _, err := Compile(source); err == nil {
			t.Fatal("unsafe fallible/coercion accepted")
		}
	}
}
