// Code generated by goplus from eval_builtins.gp. DO NOT EDIT.
//goplus:v v0.28.0

package language

import (
	"sort"
	"strings"
	"unicode"
	"unicode/utf8"

	"goforge.dev/refine/value"
)

func (e *evaluator) builtin(name string, args []evalValue, at Span) evalValue {
	switch name {
	case "not":
		return boolValue(!boolean(args[0], at))
	case "show":
		shown := e.show(args[0], at)
		e.step(uint64(len(shown)), at)
		text, err := value.TextFromUTF8(shown)
		if err != nil {
			evalError(at, "evaluation.show", "value cannot be shown as text")
		}
		return textValue(text)
	case "length":
		switch __gp_m0 := any(args[0].form).(type) {
		case evalText:
			text := __gp_m0.value
			return numberValue(value.Integer(int64(text.Length())), "Int")
		case evalList:
			items := __gp_m0.items
			return numberValue(value.Integer(int64(len(items))), "Int")
		default:
			evalError(at, "evaluation.type", "length requires text or a list")
		}
	case "reverse":
		items := itemsOf(args[0], at)
		e.step(uint64(len(items)), at)
		result := make([]evalValue, len(items))
		for i, item := range items {
			result[len(items)-1-i] = item
		}
		return evalValue{form: evalList{items: result}}
	case "map":
		items := itemsOf(args[1], at)
		e.step(uint64(len(items)), at)
		result := make([]evalValue, len(items))
		for i, item := range items {
			result[i] = e.apply(args[0], item, at)
		}
		return evalValue{form: evalList{items: result}}
	case "filter":
		items := itemsOf(args[1], at)
		e.step(uint64(len(items)), at)
		result := make([]evalValue, 0, len(items))
		for _, item := range items {
			if boolean(e.apply(args[0], item, at), at) {
				result = append(result, item)
			}
		}
		return evalValue{form: evalList{items: result}}
	case "foldl":
		result := args[1]
		for _, item := range itemsOf(args[2], at) {
			e.step(1, at)
			result = e.apply(e.apply(args[0], result, at), item, at)
		}
		return result
	case "oneOf", "elem":
		for _, item := range itemsOf(args[1], at) {
			if e.equal(args[0], item, at) {
				return boolValue(true)
			}
		}
		return boolValue(false)
	case "unique":
		items := itemsOf(args[0], at)
		for i, item := range items {
			for j := 0; j < i; j++ {
				if e.equal(item, items[j], at) {
					return boolValue(false)
				}
			}
		}
		return boolValue(true)
	case "all", "any", "satisfiesAll", "satisfiesOnlyOneOf", "satisfiesOneOf", "satisfiesAtLeastOneOf":
		return e.combine(name, args, at)
	case "read":
		evalError(at, "evaluation.type", "read requires an inferred target type")
	case "matches", "search":
		evalError(at, "evaluation.unsupported", "regex execution semantics are not implemented yet")
	}
	evalError(at, "evaluation.name", "unsupported built-in function")
	return evalValue{}
}

func (e *evaluator) attemptApply(fn, arg evalValue, at Span) (result evalValue, failure *evalFailure) {
	defer func() {
		if caught := recover(); caught != nil {
			if explained, ok := caught.(*evalFailure); ok {
				failure = explained
			} else {
				panic(caught)
			}
		}
	}()
	result = e.apply(fn, arg, at)
	return
}

// Predicate errors are unknown, not false. A decisive result survives earlier
// unknowns: e.g. any [unknown, true] and all [unknown, false]. Iteration follows
// input order and stops once the result is conclusive under three-outcome logic.
func (e *evaluator) combine(name string, args []evalValue, at Span) evalValue {
	mode := "any"
	if name == "all" || name == "satisfiesAll" {
		mode = "all"
	}
	if name == "satisfiesOnlyOneOf" {
		mode = "one"
	}
	overValues := name == "all" || name == "any"
	var items []evalValue
	if overValues {
		items = itemsOf(args[1], at)
	} else {
		items = itemsOf(args[0], at)
	}
	yes := 0
	var unknown *evalFailure
	for _, item := range items {
		fn, arg := item, args[1]
		if overValues {
			fn, arg = args[0], item
		}
		result, failure := e.attemptApply(fn, arg, at)
		if failure != nil {
			if unknown == nil {
				unknown = failure
			}
			continue
		}
		satisfied := boolean(result, at)
		if satisfied {
			yes++
		}
		if mode == "all" && !satisfied {
			return boolValue(false)
		}
		if mode == "any" && satisfied {
			return boolValue(true)
		}
		if mode == "one" && yes > 1 {
			return boolValue(false)
		}
	}
	if unknown != nil {
		panic(unknown)
	}
	if mode == "all" {
		return boolValue(true)
	}
	if mode == "one" {
		return boolValue(yes == 1)
	}
	return boolValue(false)
}

// Canonical display is separate from native serde. Records sort identifiers;
// sequences and constructor arguments keep their order. Text escapes every
// non-ASCII UTF-16 code unit. Numbers use reduced exact fractions.
func (e *evaluator) show(v evalValue, at Span) string {
	e.enter(at)
	defer func() { e.depth-- }()
	switch __gp_m1 := any(v.form).(type) {
	case evalNumber:
		n := __gp_m1.value
		e.step(uint64(len(n.Show())), at)
		return n.Show()
	case evalText:
		text := __gp_m1.value
		e.step(uint64(text.Length()), at)
		return text.Show()
	case evalBool:
		b := __gp_m1.value
		if b {
			return "True"
		}
		return "False"
	case evalList:
		items := __gp_m1.items

		e.step(uint64(len(items)), at)
		parts := make([]string, len(items))
		for i, item := range items {
			parts[i] = e.show(item, at)
		}
		return "[" + strings.Join(parts, ", ") + "]"
	case evalRecord:
		fields := __gp_m1.fields

		levels := uint64(1)
		for n := len(fields); n > 1; n >>= 1 {
			levels++
		}
		e.step(uint64(len(fields))*levels, at)
		ordered := append([]evalField(nil), fields...)
		sort.Slice(ordered, func(i, j int) bool { return ordered[i].name < ordered[j].name })
		parts := make([]string, len(ordered))
		for i, field := range ordered {
			e.step(uint64(len(field.name)), at)
			if !codecIdentifier(field.name, false) {
				evalError(at, "evaluation.show", "record field is not representable in the canonical value grammar")
			}
			parts[i] = field.name + " = " + e.show(field.value, at)
		}
		return "{" + strings.Join(parts, ", ") + "}"
	case evalVariant:
		name := __gp_m1.name
		args := __gp_m1.arguments

		e.step(uint64(len(name)+len(args)), at)
		if !codecIdentifier(name, true) {
			evalError(at, "evaluation.show", "constructor is not representable in the canonical value grammar")
		}
		if len(args) == 0 {
			return name
		}
		parts := []string{name}
		for _, arg := range args {
			shown := e.show(arg, at)
			switch __gp_m2 := any(arg.form).(type) {
			case evalNumber:
				n := __gp_m2.value
				if strings.HasPrefix(n.Show(), "-") || strings.Contains(n.Show(), "/") {
					shown = "(" + shown + ")"
				}
			default:
			}
			parts = append(parts, shown)
		}
		return "(" + strings.Join(parts, " ") + ")"
	case evalFunction:
		evalError(at, "evaluation.show", "functions do not support canonical display")
	case evalGuardedFunction:
		evalError(at, "evaluation.show", "functions do not support canonical display")
	default:
		panic("goplus: impossible enum value in match")
	}
	return ""
}

func codecIdentifier(name string, constructor bool) bool {
	if name == "" || !utf8.ValidString(name) || reserved(name) {
		return false
	}
	if constructor && (!uppercase(name) || name == "True" || name == "False") {
		return false
	}
	for i, r := range name {
		if i == 0 {
			if !unicode.IsLetter(r) && r != '_' {
				return false
			}
		} else if !unicode.IsLetter(r) && !unicode.IsDigit(r) && r != '_' && r != '\'' {
			return false
		}
	}
	return true
}

// fromData is a structural transfer, not schema validation. The caller must
// apply the declared numeric types/refinements before executing predicates.
func (e *evaluator) fromData(data value.Data, at Span) evalValue {
	e.enter(at)
	defer func() { e.depth-- }()
	switch any(data.Kind()).(type) {
	case value.NumberData:
		n, _ := data.Number()
		e.step(uint64(len(n.Show())), at)
		if n.IsInteger() {
			return numberValue(n, "Int")
		}
		return numberValue(n, "Real")
	case value.TextData:
		t, _ := data.Text()
		return textValue(t)
	case value.BoolData:
		b, _ := data.Boolean()
		return boolValue(b)
	case value.ListData:

		e.step(uint64(data.Size()), at)
		items := data.Elements()
		result := make([]evalValue, len(items))
		for i, item := range items {
			result[i] = e.fromData(item, at)
		}
		return evalValue{form: evalList{items: result}}
	case value.RecordData:

		e.step(uint64(data.Size()), at)
		fields := data.Fields()
		result := make([]evalField, len(fields))
		for i, field := range fields {
			e.step(uint64(len(field.Name)), at)
			result[i] = evalField{name: field.Name, value: e.fromData(field.Value, at)}
		}
		return evalValue{form: evalRecord{fields: result}}
	case value.VariantData:

		name, _ := data.Constructor()
		e.step(uint64(data.Size()+len(name)), at)
		args := data.Elements()
		result := make([]evalValue, len(args))
		for i, arg := range args {
			result[i] = e.fromData(arg, at)
		}
		return evalValue{form: evalVariant{name: name, arguments: result}}
	default:
		panic("goplus: impossible enum value in match")
	}
}

func (e *evaluator) toData(v evalValue, at Span) value.Data {
	e.enter(at)
	defer func() { e.depth-- }()
	switch __gp_m4 := any(v.form).(type) {
	case evalNumber:
		n := __gp_m4.value
		return value.OfNumber(n)
	case evalText:
		t := __gp_m4.value
		return value.OfText(t)
	case evalBool:
		b := __gp_m4.value
		return value.OfBool(b)
	case evalList:
		items := __gp_m4.items

		e.step(uint64(len(items)), at)
		result := make([]value.Data, len(items))
		for i, item := range items {
			result[i] = e.toData(item, at)
		}
		return value.List(result)
	case evalRecord:
		fields := __gp_m4.fields

		e.step(uint64(len(fields)), at)
		result := make([]value.DataField, len(fields))
		for i, field := range fields {
			result[i] = value.DataField{Name: field.name, Value: e.toData(field.value, at)}
		}
		data, err := value.Record(result)
		if err != nil {
			evalError(at, "evaluation.record", "invalid runtime record")
		}
		return data
	case evalVariant:
		name := __gp_m4.name
		args := __gp_m4.arguments

		e.step(uint64(len(args)), at)
		result := make([]value.Data, len(args))
		for i, arg := range args {
			result[i] = e.toData(arg, at)
		}
		data, err := value.Variant(name, result)
		if err != nil {
			evalError(at, "evaluation.constructor", "invalid runtime constructor")
		}
		return data
	case evalFunction:
		evalError(at, "evaluation.type", "a function is not a serializable payload value")
	case evalGuardedFunction:
		evalError(at, "evaluation.type", "a function is not a serializable payload value")
	default:
		panic("goplus: impossible enum value in match")
	}
	return value.Data{}
}
