// Code generated by goplus from openapi_format.gp. DO NOT EDIT.
//goplus:v v0.28.0

package language

import "strings"

func openAPIPresenceText(required *bool) string {
	if required == nil {
		return ""
	}
	if *required {
		return " required"
	}
	return " optional"
}
func openAPIFieldText(path []string, required *bool) string {
	return " at " + strings.Join(path, ".") + openAPIPresenceText(required)
}

// FormatOpenAPI emits a stable complete declaration, not a payload schema.
// As with Format, its input must be a valid parser/compiler-owned syntax tree.
func FormatOpenAPI(declaration *OpenAPIDeclaration) string {
	if declaration == nil {
		return ""
	}
	var out strings.Builder
	out.WriteString("openapi " + quote(declaration.Version) + " {\n  title " + quote(declaration.Title) + "\n  version " + quote(declaration.APIVersion) + "\n")
	body := func(item *OpenAPIBodyDeclaration) {
		if item != nil {
			out.WriteString("      body " + quote(item.MediaType) + openAPIFieldText(item.FieldPath, item.Required) + "\n")
		}
	}
	for _, operation := range declaration.Operations {
		out.WriteString("  operation " + quote(operation.OperationID) + " " + quote(operation.Method) + " " + quote(operation.Path) + " {\n    request " + operation.Request.TypeName + " {\n")
		for _, parameter := range operation.Request.Parameters {
			out.WriteString("      parameter " + parameter.In + " " + quote(parameter.Name) + openAPIFieldText(parameter.FieldPath, parameter.Required) + "\n")
		}
		body(operation.Request.Body)
		out.WriteString("    }\n")
		for _, response := range operation.Responses {
			out.WriteString("    response " + quote(response.Status) + " " + response.TypeName + " " + quote(response.Description) + " {\n")
			for _, header := range response.Headers {
				out.WriteString("      header " + quote(header.Name) + openAPIFieldText(header.FieldPath, header.Required) + "\n")
			}
			body(response.Body)
			if response.ContextType != "" {
				out.WriteString("      context " + response.ContextType + "\n")
			}
			out.WriteString("    }\n")
		}
		out.WriteString("  }\n")
	}
	out.WriteString("}\n")
	return out.String()
}
