// Code generated by goplus from diagnostic_paths.gp. DO NOT EDIT.
//goplus:v v0.28.0

package language

import "strings"

const affectedPathWorkLimit = 1000000
const affectedPathUnitLimit = 1 << 20

type affectedPathBudget struct {
	work, units, maxWork, maxUnits int
	exhausted                      bool
}

func (b *affectedPathBudget) takeWork(count int) bool {
	if count < 0 || count > b.maxWork-b.work {
		b.exhausted = true
		return false
	}
	b.work += count
	return true
}
func (b *affectedPathBudget) takeUnits(count int) bool {
	if count < 0 || count > b.maxUnits-b.units {
		b.exhausted = true
		return false
	}
	b.units += count
	return true
}

type affectedPathFrame struct {
	expression  *Expr
	rootVisible bool
}

// AffectedPaths returns the statically visible payload paths referenced by one
// where-clause predicate. It follows only projection chains rooted directly at
// the clause's `it`; named functions and local aliases are deliberately not
// expanded. When extraction exceeds its fixed work/output bounds or finds no
// narrower dependency, the enclosing path remains the conservative answer.
func AffectedPaths(predicate *Expr, enclosing string) []string {
	return affectedPathsWithLimits(predicate, enclosing, affectedPathWorkLimit, affectedPathUnitLimit)
}

func affectedPathsWithLimits(predicate *Expr, enclosing string, maxWork, maxUnits int) []string {
	budget := &affectedPathBudget{maxWork: maxWork, maxUnits: maxUnits}
	relative := affectedRelativePaths(predicate, budget)
	if budget.exhausted || len(relative) == 0 {
		return []string{enclosing}
	}
	return prefixAffectedPathsWithBudget(relative, enclosing, budget)
}

func prefixAffectedPaths(relative []string, enclosing string) []string {
	return prefixAffectedPathsWithLimits(relative, enclosing, affectedPathWorkLimit, affectedPathUnitLimit)
}
func prefixAffectedPathsWithLimits(relative []string, enclosing string, maxWork, maxUnits int) []string {
	return prefixAffectedPathsWithBudget(relative, enclosing, &affectedPathBudget{maxWork: maxWork, maxUnits: maxUnits})
}
func prefixAffectedPathsWithBudget(relative []string, enclosing string, budget *affectedPathBudget) []string {
	fallback := func() []string { return []string{enclosing} }
	if len(relative) == 0 || budget == nil || !budget.takeWork(len(relative)) {
		return fallback()
	}
	if len(enclosing) > 0 && len(relative) > budget.maxUnits/len(enclosing) {
		budget.exhausted = true
		return fallback()
	}
	if !budget.takeUnits(len(enclosing) * len(relative)) {
		return fallback()
	}
	for _, path := range relative {
		if !budget.takeUnits(len(path)) {
			return fallback()
		}
	}
	result := make([]string, len(relative))
	for i, path := range relative {
		result[i] = enclosing + path
	}
	return result
}

func affectedRelativePaths(predicate *Expr, budget *affectedPathBudget) []string {
	work := []affectedPathFrame{}
	queued := map[affectedPathFrame]bool{}
	relative := []string{}
	seen := map[string]bool{}
	push := func(expression *Expr, visible bool) {
		current := affectedPathFrame{expression, visible}
		if expression == nil || queued[current] || !budget.takeWork(1) {
			return
		}
		queued[current] = true
		work = append(work, current)
	}
	add := func(path string) {
		if !seen[path] {
			seen[path] = true
			relative = append(relative, path)
		}
	}
	push(predicate, true)
	for len(work) > 0 && !budget.exhausted {
		current := work[len(work)-1]
		work = work[:len(work)-1]
		if current.rootVisible {
			if path, ok := directAffectedPath(current.expression, budget); ok {
				add(path)
				continue
			}
		}
		switch __gp_m0 := any(current.expression.Form).(type) {
		case NumberLiteral:

		case TextLiteral:

		case BoolLiteral:

		case Variable:

		case Project:
			record := __gp_m0.Record
			push(record, current.rootVisible)
		case Unary:
			operand := __gp_m0.Operand
			push(operand, current.rootVisible)
		case Binary:
			left := __gp_m0.Left
			right := __gp_m0.Right
			push(right, current.rootVisible)
			push(left, current.rootVisible)
		case Apply:
			function := __gp_m0.Function
			argument := __gp_m0.Argument
			push(argument, current.rootVisible)
			push(function, current.rootVisible)
		case Conditional:
			condition := __gp_m0.Condition
			yes := __gp_m0.Then
			no := __gp_m0.Else
			push(no, current.rootVisible)
			push(yes, current.rootVisible)
			push(condition, current.rootVisible)
		case Let:
			name := __gp_m0.Name
			bound := __gp_m0.Value
			body := __gp_m0.Body
			push(body, current.rootVisible && name != "it")
			push(bound, current.rootVisible)
		case ListLiteral:
			items := __gp_m0.Elements
			for i := len(items) - 1; i >= 0; i-- {
				push(items[i], current.rootVisible)
			}
		case RecordLiteral:
			fields := __gp_m0.Fields
			for i := len(fields) - 1; i >= 0; i-- {
				push(fields[i].Value, current.rootVisible)
			}
		case MapLiteral:
			entries := __gp_m0.Entries
			for i := len(entries) - 1; i >= 0; i-- {
				push(entries[i].Value, current.rootVisible)
			}
		case Case:
			subject := __gp_m0.Value
			arms := __gp_m0.Arms

			for i := len(arms) - 1; i >= 0; i-- {
				binds := diagnosticPatternBindsIt(arms[i].Pattern, budget)
				push(arms[i].Body, current.rootVisible && !binds)
			}
			push(subject, current.rootVisible)
		default:
			panic("goplus: impossible enum value in match")
		}
	}
	return relative
}

func directAffectedPath(expression *Expr, budget *affectedPathBudget) (string, bool) {
	fields := []string{}
	current := expression
	seen := map[*Expr]bool{}
	for current != nil {
		if seen[current] || !budget.takeWork(1) {
			return "", false
		}
		seen[current] = true
		switch __gp_m1 := any(current.Form).(type) {
		case Project:
			record := __gp_m1.Record
			field := __gp_m1.Field
			if !budget.takeUnits(len(field)) {
				return "", false
			}
			fields = append(fields, field)
			current = record
		case Variable:
			name := __gp_m1.Name

			if name != "it" {
				return "", false
			}
			size := 0
			for i := len(fields) - 1; i >= 0; i-- {
				next := 1 + len(fields[i])
				for j := 0; j < len(fields[i]); j++ {
					if fields[i][j] == '~' || fields[i][j] == '/' {
						next++
					}
				}
				if !budget.takeUnits(next) {
					return "", false
				}
				size += next
			}
			var path strings.Builder
			path.Grow(size)
			for i := len(fields) - 1; i >= 0; i-- {
				path.WriteByte('/')
				for j := 0; j < len(fields[i]); j++ {
					switch fields[i][j] {
					case '~':
						path.WriteString("~0")
					case '/':
						path.WriteString("~1")
					default:
						path.WriteByte(fields[i][j])
					}
				}
			}
			return path.String(), true
		default:
			return "", false
		}
	}
	return "", false
}

func diagnosticPatternBindsIt(pattern *Pattern, budget *affectedPathBudget) bool {
	work := []*Pattern{}
	seen := map[*Pattern]bool{}
	push := func(item *Pattern) {
		if item == nil || seen[item] || !budget.takeWork(1) {
			return
		}
		seen[item] = true
		work = append(work, item)
	}
	push(pattern)
	for len(work) > 0 && !budget.exhausted {
		current := work[len(work)-1]
		work = work[:len(work)-1]
		switch __gp_m2 := any(current.Form).(type) {
		case BindPattern:
			name := __gp_m2.Name
			if name == "it" {
				return true
			}
		case ConstructorPattern:
			arguments := __gp_m2.Arguments
			for _, argument := range arguments {
				push(argument)
			}
		case ListPattern:
			items := __gp_m2.Elements
			for _, item := range items {
				push(item)
			}
		case ConsPattern:
			head := __gp_m2.Head
			tail := __gp_m2.Tail
			push(head)
			push(tail)
		case WildPattern:

		case LiteralPattern:

		default:
			panic("goplus: impossible enum value in match")
		}
	}
	return false
}
