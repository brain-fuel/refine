// Code generated by goplus from project_ordinary_test.gp. DO NOT EDIT.
//goplus:v v0.28.0

package native

import (
	"goforge.dev/refine/schemajson"
	"strings"
	"testing"
)

func TestOrdinaryProjectExportErasesOnlyRefineSchemaAnnotations(t *testing.T) {
	source := `{"type":"integer","examples":[{"x-refine":"literal, not code"}],"x-refine":{"source":"type Root = Int where it > 0\n","root":"Root"},"$defs":{"Other":{"type":"string","x-refine":"type Other = String"}}}`
	p, err := IngestProject(JSONSchema, []byte(source), ProjectOptions{Root: ResourceSelector{TypeName: "Root"}})
	if err != nil {
		t.Fatal(err)
	}
	ordinary, err := p.Export(LowerOptions{Mode: Ordinary, AllowDocumentedLoss: true})
	if err != nil {
		t.Fatal(err)
	}
	doc, err := schemajson.Parse([]byte(ordinary.Resources()[0].Source), schemajson.Limits{})
	if err != nil {
		t.Fatal(err)
	}
	for _, pointer := range []string{"/x-refine", "/$defs/Other/x-refine"} {
		if _, err := doc.At(pointer); err == nil {
			t.Fatal("ordinary schema retained executable refinement", pointer)
		}
	}
	literal, err := doc.At("/examples/0/x-refine")
	if err != nil || literal.Raw() != `"literal, not code"` {
		t.Fatal("literal example altered", err)
	}
	if p.Resources()[0].Source != source {
		t.Fatal("immutable original changed")
	}
	refined, err := p.Export(LowerOptions{Mode: Refined})
	if err != nil {
		t.Fatal(err)
	}
	if !strings.Contains(refined.Resources()[0].Source, `"x-refine"`) {
		t.Fatal("refined export lost executable source")
	}
}
