// Code generated by goplus from document.gp. DO NOT EDIT.
//goplus:v v0.28.0

// Package native validates and retains ordinary and Refined native schema documents.
// It is deliberately offline: ingestion never reads a referenced file or URL.
package native

import (
	"context"
	"errors"
	"fmt"
	"io"
	"regexp"
	"strings"

	"github.com/getkin/kin-openapi/openapi3"
	avro "github.com/hamba/avro/v2"
	yaml "github.com/oasdiff/yaml3"
	jsonoracle "github.com/santhosh-tekuri/jsonschema/v6"

	"goforge.dev/refine/provenance"
	"goforge.dev/refine/schemajson"
)

type Format string

const (
	JSONSchema Format = "json-schema"
	Avro       Format = "avro"
	OpenAPI    Format = "openapi"
)

// Limits bound syntax ingestion. Zero selects schemajson's documented default.
type Options struct{ Limits schemajson.Limits }

type Error struct {
	Code    string
	Format  Format
	Pointer string
	Message string
	Cause   error
}

func (e *Error) Error() string {
	at := ""
	if e.Pointer != "" {
		at = " at " + e.Pointer
	}
	return e.Code + " (" + string(e.Format) + ")" + at + ": " + e.Message
}
func (e *Error) Unwrap() error { return e.Cause }

// Annotation is a checked x-refine extension. Source is either a complete
// language module, or the source member of {source,root}. Root, when present,
// is checked as a closed payload type in that module.
type Annotation struct {
	Pointer     string
	Source      string
	Root        string
	Formatted   string
	Metadata    WireMetadata
	HasMetadata bool
}

// Document retains the exact caller bytes. Parsed oracle models are not exposed:
// doing so would make an apparently immutable imported contract mutable.
type Document struct {
	format         Format
	version        string
	raw            string
	annotations    []Annotation
	jsonProvenance *provenance.JSONSchema
}

func (d *Document) Format() Format {
	if d == nil {
		return ""
	}
	return d.format
}
func (d *Document) Version() string {
	if d == nil {
		return ""
	}
	return d.version
}
func (d *Document) Original() string {
	if d == nil {
		return ""
	}
	return d.raw
}
func (d *Document) ExportOriginal() []byte {
	if d == nil {
		return nil
	}
	return append([]byte(nil), []byte(d.raw)...)
}
func (d *Document) Annotations() []Annotation {
	if d == nil {
		return nil
	}
	out := append([]Annotation(nil), d.annotations...)
	for i := range out {
		out[i].Metadata = copyMetadata(out[i].Metadata)
	}
	return out
}

func Parse(format Format, input []byte, options Options) (*Document, error) {
	switch format {
	case JSONSchema:
		return ParseJSONSchema(input, options)
	case Avro:
		return ParseAvro(input, options)
	case OpenAPI:
		return ParseOpenAPI(input, options)
	default:
		return nil, &Error{Code: "native.format", Format: format, Message: "supported formats are json-schema, avro, and openapi"}
	}
}

func limits(options Options) (schemajson.Limits, error) {
	l := options.Limits
	if l.Bytes < 0 || l.Depth < 0 || l.Nodes < 0 {
		return l, errors.New("limits must be nonnegative")
	}
	if l.Bytes == 0 {
		l.Bytes = schemajson.DefaultBytes
	}
	if l.Depth == 0 {
		l.Depth = schemajson.DefaultDepth
	}
	if l.Nodes == 0 {
		l.Nodes = schemajson.DefaultNodes
	}
	return l, nil
}

func ParseJSONSchema(input []byte, options Options) (document *Document, failure error) {
	defer recoverRegexEvaluation(JSONSchema, &failure)
	doc, err := schemajson.Parse(input, options.Limits)
	if err != nil {
		return nil, wrap(JSONSchema, "native.syntax", "", err)
	}
	if err := scalarJSON(doc.Root(), ""); err != nil {
		return nil, wrap(JSONSchema, "native.encoding", "", err)
	}
	root := doc.Root()
	kind := schemajson.KindName(root.Kind())
	if kind != "object" && kind != "boolean" {
		return nil, &Error{Code: "native.structure", Format: JSONSchema, Message: "a Draft 2020-12 schema must be an object or Boolean"}
	}
	if err := checkJSONDraft(root, ""); err != nil {
		return nil, err
	}
	compiler := jsonoracle.NewCompiler()
	compiler.DefaultDraft(jsonoracle.Draft2020)
	compiler.UseRegexpEngine(jsonRegexp)
	// No URLLoader is installed. Internal references work; external references
	// fail closed instead of causing filesystem or network access.
	oracleInput, err := jsonoracle.UnmarshalJSON(strings.NewReader(doc.Raw()))
	if err != nil {
		return nil, wrap(JSONSchema, "native.syntax", "", err)
	}
	if err := compiler.AddResource("https://refine.invalid/imported.schema.json", oracleInput); err != nil {
		return nil, wrap(JSONSchema, "native.structure", "", err)
	}
	if _, err := compiler.Compile("https://refine.invalid/imported.schema.json"); err != nil {
		return nil, wrap(JSONSchema, "native.structure", "", err)
	}
	annotations, err := jsonSchemaAnnotations(root)
	if err != nil {
		return nil, err
	}
	origins, err := provenance.DiscoverJSONSchema(input, options.Limits)
	if err != nil {
		return nil, wrap(JSONSchema, "native.provenance", "", err)
	}
	return &Document{format: JSONSchema, version: "2020-12", raw: doc.Raw(), annotations: annotations, jsonProvenance: origins}, nil
}

func ParseAvro(input []byte, options Options) (*Document, error) {
	doc, err := schemajson.Parse(input, options.Limits)
	if err != nil {
		return nil, wrap(Avro, "native.syntax", "", err)
	}
	if err := scalarJSON(doc.Root(), ""); err != nil {
		return nil, wrap(Avro, "native.encoding", "", err)
	}
	kind := schemajson.KindName(doc.Root().Kind())
	if kind != "object" && kind != "array" && kind != "string" {
		return nil, &Error{Code: "native.structure", Format: Avro, Message: "an Avro schema must be a JSON string, object, or array"}
	}
	// A private cache prevents schemas from one document affecting another.
	schema, err := parseAvroStructure(input, &avro.SchemaCache{})
	if err != nil {
		return nil, wrap(Avro, "native.structure", "", err)
	}
	if err := auditAvroDefaults(doc.Root(), schema, ""); err != nil {
		return nil, err
	}
	annotations, err := avroAnnotations(doc.Root())
	if err != nil {
		return nil, err
	}
	return &Document{format: Avro, version: "1.12.0", raw: doc.Raw(), annotations: annotations}, nil
}

var openAPIVersion = regexp.MustCompile(`^3\.(0|1|2)\.\d+$`)

func supportedOpenAPI(version string) bool {
	switch version {
	case "3.0.0", "3.0.1", "3.0.2", "3.0.3", "3.0.4", "3.1.0", "3.1.1", "3.1.2", "3.2.0", "3.2.1":
		return true
	}
	return false
}

func declaredOpenAPIVersion(root *yaml.Node) (string, bool) {
	if root == nil || root.Kind != yaml.MappingNode {
		return "", false
	}
	for i := 0; i < len(root.Content); i += 2 {
		if root.Content[i].Value == "openapi" {
			value := root.Content[i+1]
			return value.Value, value.Kind == yaml.ScalarNode
		}
	}
	return "", false
}

// kin-openapi v0.149.0 recognizes the 3.2 feature set only for the then-current
// 3.2.0 patch string. OAS patch releases clarify/fix the specification without
// changing that feature set. Normalize only the private oracle input to 3.2.0;
// the accepted 3.2.1 source and reported version remain untouched.
func openAPIOracleInput(input []byte, root *yaml.Node, version string) ([]byte, error) {
	if version != "3.2.1" {
		return input, nil
	}
	for i := 0; i < len(root.Content); i += 2 {
		if root.Content[i].Value == "openapi" {
			value := root.Content[i+1]
			original := value.Value
			value.Value = "3.2.0"
			data, err := yaml.Marshal(root)
			value.Value = original
			return data, err
		}
	}
	return nil, errors.New("OpenAPI object requires an openapi version")
}

func ParseOpenAPI(input []byte, options Options) (document *Document, failure error) {
	defer recoverRegexEvaluation(OpenAPI, &failure)
	l, err := limits(options)
	if err != nil {
		return nil, wrap(OpenAPI, "native.limit", "", err)
	}
	if len(input) > l.Bytes {
		return nil, &Error{Code: "native.limit", Format: OpenAPI, Message: "document byte limit exceeded"}
	}
	if strings.HasPrefix(strings.TrimSpace(string(input)), "{") {
		jsonDoc, err := schemajson.Parse(input, options.Limits)
		if err != nil {
			return nil, wrap(OpenAPI, "native.syntax", "", err)
		}
		if err := scalarJSON(jsonDoc.Root(), ""); err != nil {
			return nil, wrap(OpenAPI, "native.encoding", "", err)
		}
	}
	yamlRoot, err := parseYAML(input, l)
	if err != nil {
		return nil, wrap(OpenAPI, "native.syntax", "", err)
	}
	version, ok := declaredOpenAPIVersion(yamlRoot)
	if !ok || !openAPIVersion.MatchString(version) || !supportedOpenAPI(version) {
		return nil, &Error{Code: "native.version", Format: OpenAPI, Pointer: "/openapi", Message: "supported published versions are 3.0.0-3.0.4, 3.1.0-3.1.2, and 3.2.0-3.2.1"}
	}
	oracleInput, err := openAPIOracleInput(input, yamlRoot, version)
	if err != nil {
		return nil, wrap(OpenAPI, "native.structure", "", err)
	}
	loader := openapi3.NewLoader()
	loader.IsExternalRefsAllowed = false
	parsed, err := loader.LoadFromData(oracleInput)
	if err != nil {
		return nil, wrap(OpenAPI, "native.structure", "", err)
	}
	if err := parsed.Validate(context.Background(), openapi3.SetRegexCompiler(openAPIRegexp)); err != nil {
		return nil, wrap(OpenAPI, "native.structure", "", err)
	}
	annotations, err := yamlRootAnnotation(yamlRoot)
	if err != nil {
		return nil, err
	}
	return &Document{format: OpenAPI, version: version, raw: string(append([]byte(nil), input...)), annotations: annotations}, nil
}

func wrap(format Format, code, pointer string, err error) *Error {
	return &Error{Code: code, Format: format, Pointer: pointer, Message: err.Error(), Cause: err}
}

func parseYAML(input []byte, l schemajson.Limits) (*yaml.Node, error) {
	numericExpansion := 0
	return parseYAMLWithNumericExpansion(input, l, &numericExpansion)
}
func parseYAMLWithNumericExpansion(input []byte, l schemajson.Limits, numericExpansion *int) (*yaml.Node, error) {
	decoder := yaml.NewDecoder(strings.NewReader(string(input)))
	var root yaml.Node
	if err := decoder.Decode(&root); err != nil {
		return nil, err
	}
	if len(root.Content) == 0 {
		return nil, errors.New("empty document")
	}
	var extra yaml.Node
	if err := decoder.Decode(&extra); err != io.EOF {
		return nil, errors.New("OpenAPI input must contain exactly one YAML document")
	}
	count := 0
	if err := checkYAML(root.Content[0], 0, l, &count, numericExpansion, "", make(map[*yaml.Node]bool)); err != nil {
		return nil, err
	}
	return root.Content[0], nil
}

func checkYAML(node *yaml.Node, depth int, l schemajson.Limits, count, numericExpansion *int, path string, active map[*yaml.Node]bool) error {
	if depth > l.Depth {
		return fmt.Errorf("document depth limit exceeded at %s", path)
	}
	*count++
	if *count > l.Nodes {
		return errors.New("document node limit exceeded")
	}
	if active[node] {
		return fmt.Errorf("cyclic YAML alias at %s", path)
	}
	active[node] = true
	defer delete(active, node)
	if node.Kind == yaml.AliasNode {
		return fmt.Errorf("YAML aliases are not supported at %s; expand the value explicitly", path)
	}
	if node.Kind == yaml.MappingNode {
		seen := make(map[string]bool)
		for i := 0; i < len(node.Content); i += 2 {
			key := node.Content[i]
			if key.Kind != yaml.ScalarNode {
				return fmt.Errorf("non-scalar mapping key at %s", path)
			}
			if key.Value == "<<" {
				return fmt.Errorf("YAML merge keys are not supported at %s; expand the mapping explicitly", path)
			}
			if seen[key.Value] {
				return fmt.Errorf("duplicate mapping key %q at %s", key.Value, path)
			}
			seen[key.Value] = true
			childPath := path + "/" + escapePointer(key.Value)
			if err := checkYAML(node.Content[i+1], depth+1, l, count, numericExpansion, childPath, active); err != nil {
				return err
			}
		}
		return nil
	}
	if node.Kind == yaml.ScalarNode && (node.Tag == "!!int" || node.Tag == "!!float") {
		if len(node.Value) > DefaultNumericExpansion {
			return fmt.Errorf("exact YAML number token limit exceeded at %s", path)
		}
		raw := ""
		var err error
		if node.Tag == "!!int" {
			raw, err = yamlIntegerJSON(node.Value)
		} else {
			raw, err = yamlFloatJSON(node.Value)
		}
		if err != nil {
			return err
		}
		cost, err := exactJSONNumberExpansion(raw, path, DefaultNumericExpansion)
		if err != nil {
			return err
		}
		if cost > DefaultNumericExpansion-*numericExpansion {
			return fmt.Errorf("aggregate exact YAML number expansion limit exceeded at %s", path)
		}
		*numericExpansion += cost
	}
	for i, child := range node.Content {
		if err := checkYAML(child, depth+1, l, count, numericExpansion, fmt.Sprintf("%s/%d", path, i), active); err != nil {
			return err
		}
	}
	return nil
}

func escapePointer(s string) string {
	return strings.ReplaceAll(strings.ReplaceAll(s, "~", "~0"), "/", "~1")
}
func nodeString(node schemajson.Node) (string, bool) {
	text, ok := node.Text()
	if !ok {
		return "", false
	}
	s, err := text.UTF8()
	return s, err == nil
}
func checkJSONDraft(root schemajson.Node, prefix string) error {
	if schemajson.KindName(root.Kind()) == "object" {
		if dialect, ok := root.Lookup("$schema"); ok {
			value, ok := nodeString(dialect)
			if !ok || strings.TrimSuffix(value, "#") != "https://json-schema.org/draft/2020-12/schema" {
				return &Error{Code: "native.version", Format: JSONSchema, Pointer: prefix + "/$schema", Message: "only JSON Schema Draft 2020-12 is supported"}
			}
		}
	}
	return nil
}

func scalarJSON(node schemajson.Node, path string) error {
	return scalarJSONWithNumericExpansion(node, path, DefaultNumericExpansion)
}
func scalarJSONWithNumericExpansion(node schemajson.Node, path string, limit int) error {
	used := 0
	return scalarJSONBudget(node, path, limit, &used)
}
func scalarJSONText(node schemajson.Node, path string) error {
	switch schemajson.KindName(node.Kind()) {
	case "string":
		if _, ok := nodeString(node); !ok {
			return fmt.Errorf("non-scalar Unicode string at %s is not supported by native validators", path)
		}
	case "array":
		for i, child := range node.Elements() {
			if err := scalarJSONText(child, fmt.Sprintf("%s/%d", path, i)); err != nil {
				return err
			}
		}
	case "object":
		for _, member := range node.Members() {
			key, err := member.Key.UTF8()
			if err != nil {
				return fmt.Errorf("non-scalar Unicode object key at %s is not supported by native validators", path)
			}
			if err := scalarJSONText(member.Value, path+"/"+escapePointer(key)); err != nil {
				return err
			}
		}
	}
	return nil
}
func scalarJSONBudget(node schemajson.Node, path string, limit int, used *int) error {
	switch schemajson.KindName(node.Kind()) {
	case "string":
		if _, ok := nodeString(node); !ok {
			return fmt.Errorf("non-scalar Unicode string at %s is not supported by native validators", path)
		}
	case "number":
		cost, err := exactJSONNumberExpansion(node.Raw(), path, limit)
		if err != nil {
			return err
		}
		if cost > limit-*used {
			return fmt.Errorf("aggregate exact JSON number expansion limit exceeded at %s", path)
		}
		*used += cost
	case "array":
		for i, child := range node.Elements() {
			if err := scalarJSONBudget(child, fmt.Sprintf("%s/%d", path, i), limit, used); err != nil {
				return err
			}
		}
	case "object":
		for _, member := range node.Members() {
			key, err := member.Key.UTF8()
			if err != nil {
				return fmt.Errorf("non-scalar Unicode object key at %s is not supported by native validators", path)
			}
			if err := scalarJSONBudget(member.Value, path+"/"+escapePointer(key), limit, used); err != nil {
				return err
			}
		}
	}
	return nil
}

// exactJSONNumberExpansion charges the token bytes plus the absolute decimal
// exponent without constructing a big integer. Callers aggregate this cost
// across the complete document before handing any value to an exact oracle.
func exactJSONNumberExpansion(raw, path string, limit int) (int, error) {
	if limit <= 0 {
		return 0, fmt.Errorf("exact JSON number expansion limit must be positive")
	}
	if len(raw) > limit {
		return 0, fmt.Errorf("exact JSON number token limit exceeded at %s", path)
	}
	exponent := ""
	if index := strings.IndexAny(raw, "eE"); index >= 0 {
		exponent = raw[index+1:]
	}
	if strings.HasPrefix(exponent, "+") || strings.HasPrefix(exponent, "-") {
		exponent = exponent[1:]
	}
	magnitude := 0
	for _, char := range exponent {
		if char < '0' || char > '9' {
			return 0, fmt.Errorf("invalid JSON number exponent at %s", path)
		}
		digit := int(char - '0')
		if magnitude > (limit-digit)/10 {
			return 0, fmt.Errorf("exact JSON number exponent limit exceeded at %s", path)
		}
		magnitude = magnitude*10 + digit
	}
	if magnitude > limit-len(raw) {
		return 0, fmt.Errorf("exact JSON number expansion limit exceeded at %s", path)
	}
	return len(raw) + magnitude, nil
}
