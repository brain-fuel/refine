// Code generated by goplus from schema_locations_json.gp. DO NOT EDIT.
//goplus:v v0.28.0

package native

import (
	"sort"

	"goforge.dev/refine/schemajson"
)

// Every explicit JSON Schema resource is part of the scan, including unused
// definitions. Index once so aliases never cause duplicate visits, and use the
// same $id/anchor resolution as editable projection. Example/default payloads
// are not Schema Objects and therefore cannot inject validator requirements.
func walkCatalogJSONSchemaLocations(resources []Resource, visit func(string, string, schemajson.Node) error) error {
	catalog, err := newJSONProjectionCatalog(resources)
	if err != nil {
		return err
	}
	keys := make([]string, 0, len(catalog.locations))
	for key := range catalog.locations {
		keys = append(keys, key)
	}
	sort.Strings(keys)
	for _, key := range keys {
		current := catalog.locations[key]
		if schemajson.KindName(current.node.Kind()) == "boolean" {
			continue
		}
		if reference, ok := current.node.Lookup("$ref"); ok {
			raw, ok := nodeString(reference)
			if !ok {
				return &Error{Code: "native.projection", Format: JSONSchema, Pointer: key + "/$ref", Message: "schema reference must be text"}
			}
			if _, err := catalog.resolve(current, raw); err != nil {
				return err
			}
		}
		if err := visit(current.resource, current.pointer, current.node); err != nil {
			return err
		}
	}
	return nil
}
