// Code generated by goplus from openapi_explanation.gp. DO NOT EDIT.
//goplus:v v0.28.0

package native

import (
	"encoding/json"
	"strings"

	"goforge.dev/refine/explain"
	"goforge.dev/refine/language"
	refineopenapi "goforge.dev/refine/openapi"
)

// OpenAPIContractExplanation describes explicitly authored operation bindings
// and every checked declaration they may reference. The native schema does not
// by itself enforce these cross-request rules.
func OpenAPIContractExplanation(program *language.Program, metadata WireMetadata) (string, error) {
	if metadata.OpenAPI == nil {
		return "", nil
	}
	checked, err := refineopenapi.Compile(program, metadata.OpenAPI)
	if err != nil {
		return "", err
	}
	document, err := explain.Generate(program)
	if err != nil {
		return "", err
	}
	schema := checked.Schema()
	encoded, err := json.MarshalIndent(schema, "", "  ")
	if err != nil {
		return "", err
	}
	nativeDirection := ""
	if metadata.OpenAPI.Native != nil {
		nativeDirection = " For OpenAPI 3.0 native bindings, required read-only properties are required only in responses and required write-only properties only in requests. A wrong-direction property that is nevertheless supplied remains permitted by the schema and its value is validated; OpenAPI advises that it SHOULD NOT be sent."
	}
	mapping := "# OpenAPI operation refinement bindings\n\nThese explicitly authored bindings connect HTTP operation descriptors to checked Refine request, response, and context types. The ordinary native schema does not structurally enforce this mapping." + nativeDirection + " A response binding with a context type requires the original request. Validation treats missing required original-request context as indeterminate; a separately proven response violation remains invalid and is marked incomplete. Invalid and indeterminate outcomes both prevent normal use.\n\n## Checked operation mapping\n\n" + openAPIMarkdownFence("json", string(encoded)) + "## Complete checked Refine contract\n\n" + document.Markdown()
	return mapping, nil
}

func openAPIMarkdownFence(language, text string) string {
	delimiter := "```"
	for strings.Contains(text, delimiter) {
		delimiter += "`"
	}
	return delimiter + language + "\n" + text + "\n" + delimiter + "\n\n"
}

func openAPIProjectLoss() Loss {
	return Loss{Owner: "$openapi", Location: "operation metadata", Predicate: "the checked OpenAPI request, response, and original-request context bindings", Explanation: "The ordinary native schema does not structurally enforce operation IDs, methods, paths, response selection, or cross-request Refine predicates. A refinement-aware validator must apply the versioned operation metadata and treat missing required original-request context as indeterminate."}
}

func projectOpenAPIExplanation(program *language.Program, metadata WireMetadata, companion string, losses []Loss) (string, []Loss, error) {
	operation, err := OpenAPIContractExplanation(program, metadata)
	if err != nil {
		return "", nil, err
	}
	if operation == "" {
		return companion, losses, nil
	}
	if len(losses) > 0 {
		encoded, err := json.MarshalIndent(losses, "", "  ")
		if err != nil {
			return "", nil, err
		}
		operation += "\n## Native export-specific enforcement losses\n\n" + openAPIMarkdownFence("json", string(encoded))
	}
	loss := openAPIProjectLoss()
	return operation, append(losses, loss), nil
}
